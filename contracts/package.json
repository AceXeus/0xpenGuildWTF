{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.17",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat ignition deploy ./ignition/modules/NFTMarketplace.ts --network moonbaseAlpha"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
